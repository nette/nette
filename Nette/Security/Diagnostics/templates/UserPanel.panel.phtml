<?php

namespace Nette\Security\Diagnostics;

use Nette;

?>
<div class="nette-UserPanel">
	<h1><?php if ($user->isLoggedIn()): ?>Logged in<?php else: ?>Unlogged<?php endif ?></h1>

	<?php if ($user->getIdentity()): echo Nette\Diagnostics\Dumper::toHtml($user->getIdentity()); else: ?><p>no identity</p><?php endif ?>
</div>
