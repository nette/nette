{form myForm}
<table>
{foreach $form->controls as $name => $field}
<tr>
	<th>{label $name /}<th>
	<td>{input $name title => Hello, cols => 10}</td>
</tr>
{/foreach}
</table>
{/form myForm}


{form myForm}
	<LABEL n:name="username">Name</LABEL>
	<input value=val type class="hello" n:name="username">

	<input n:input="$form[username]">
{/form myForm}


{form myForm}
	{foreach $form[sex]->items as $key => $label}
		{label sex:$key/}{input sex:$key} {$label}
	{/foreach}
{/form myForm}
